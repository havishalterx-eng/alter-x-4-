import { MiddlewareConsumer, Module, NestModule } from "@nestjs/common"; import { Pool } from "pg"; import { StaffController } from "./staff.controller"; import { StaffAuthMiddleware } from "./staff.middleware"; import { StaffRepository } from "./staff.repository"; import { StaffService } from "./staff.service";
@Module({controllers:[StaffController],providers:[{provide:StaffRepository,useFactory:()=>new StaffRepository(new Pool({connectionString:process.env.DATABASE_URL}))},StaffService,StaffAuthMiddleware]}) export class StaffModule implements NestModule { configure(consumer:MiddlewareConsumer){consumer.apply(StaffAuthMiddleware).forRoutes(StaffController);} }
